@component-name = "portal-wcm"
definition {

	property portal.release = "true";
	property portal.upstream = "true";
	property testray.main.component.name = "Web Content Administration";

	setUp {
		TestCase.setUpPortalInstance();

		User.firstLoginPG();

		JSONGroup.addGroup(groupName = "Test Site Name");
	}

	tearDown {
		var testPortalInstance = PropsUtil.get("test.portal.instance");

		if ("${testPortalInstance}" == "true") {
			PortalInstances.tearDownCP();
		}
		else {
			JSONGroup.deleteGroupByName(groupName = "Test Site Name");
		}
	}

	@description = "This is a test for LPS-123536. As Content Creator, I want to define reusable translations when creating WEM Structures with nested field"
	@ignore = "true"
	@priority = "4"
	test CheckDefaultStructureTitleDisplayedOnStructureList {
		property portal.acceptance = "false";

		// TODO LPS-123536 CheckDefaultStructureTitleDisplayedOnStructureList pending implementation

	}

	@description = "This is a test for LPS-123536. As Content Creator, I want to define reusable translations when creating WEM Structures with nested field"
	@ignore = "true"
	@priority = "4"
	test CheckLanguageSelector {
		property portal.acceptance = "false";

		// TODO LPS-123536 CheckLanguageSelector pending implementation
	}

	@description = "This is a test for LPS-123536. As Content Creator, I want to define reusable translations when creating WEM Structures with nested field"
	@ignore = "true"
	@priority = "5"
	test CheckLocalizationOfStructureUponLanguageChange {
		property portal.acceptance = "false";

		// TODO LPS-123536 CheckLocalizationOfStructureUponLanguageChange pending implementation
	}

	@description = "This is a test for LPS-123536. As Content Creator, I want to define reusable translations when creating WEM Structures with nested field"
	@ignore = "true"
	@priority = "4"
	test DefaultTitleIsDisplayedUponLocaleChange {
		property portal.acceptance = "false";

		// TODO LPS-123536 DefaultTitleIsDisplayedUponLocaleChange pending implementation
	}

	@description = "This is a test for LPS-123536. As Content Creator, I want to define reusable translations when creating WEM Structures with nested field"
	@ignore = "true"
	@priority = "4"
	test DefaultTitlePersistsAfterTranslatingForAnotherLocale {
		property portal.acceptance = "false";

		// TODO LPS-123536 DefaultTitlePersistsAfterTranslatingForAnotherLocale pending implementation
	}

	@description = "This is a test for LPS-123536. As Content Creator, I want to define reusable translations when creating WEM Structures with nested field"
	@ignore = "true"
	@priority = "4"
	test DefaultTitlePersistsUponFirstlyTranslatingForAnotherLocale {
		property portal.acceptance = "false";

		// TODO LPS-123536 DefaultTitlePersistsUponFirstlyTranslatingForAnotherLocale pending implementation
	}

	@description = "This is a test for LPS-123536. As Content Creator, I want to define reusable translations when creating WEM Structures with nested field"
	@ignore = "true"
	@priority = "4"
	test DuplicateTranslatedField {
		property portal.acceptance = "false";

		// TODO LPS-123536 DuplicateTranslatedField pending implementation
	}

	@description = "This is a test for LPS-123536. As Content Creator, I want to define reusable translations when creating WEM Structures with nested field"
	@ignore = "true"
	@priority = "5"
	test TranslateStructureWithAllFieldsNested {
		property portal.acceptance = "false";

		// TODO LPS-123536 TranslateStructureWithAllFieldsNested pending implementation
	}

	@description = "This is a test for LPS-123536. As Content Creator, I want to define reusable translations when creating WEM Structures with nested field"
	@ignore = "true"
	@priority = "4"
	test TranslateStructureWithoutDefaultTitle {
		property portal.acceptance = "false";

		// TODO LPS-123536 TranslateStructureWithoutDefaultTitle pending implementation
	}

	@description = "This is a test for LPS-123536. As Content Creator, I want to define reusable translations when creating WEM Structures with nested field"
	@ignore = "true"
	@priority = "5"
	test TranslationDoesNotChangeAnother {
		property portal.acceptance = "false";

		// TODO LPS-123536 TranslationDoesNotChangeAnother pending implementation
	}

	@description = "This is a test for LPS-123536. As Content Creator, I want to define reusable translations when creating WEM Structures with nested field"
	@ignore = "true"
	@priority = "5"
	test TranslationPersistsAfterEditingAnother {
		property portal.acceptance = "false";

		// TODO LPS-123536 TranslationPersistsAfterEditingAnother pending implementation
	}

	@description = "This is a test for LPS-123536. As Content Creator, I want to define reusable translations when creating WEM Structures with nested field"
	@ignore = "true"
	@priority = "5"
	test TranslationPersistsForTwoLevelNestedFields {
		property portal.acceptance = "false";

		// TODO LPS-123536 TranslationPersistsForTwoLevelNestedFields pending implementation
	}

	@description = "This is a test for LPS-123536. As Content Creator, I want to define reusable translations when creating WEM Structures with nested field"
	@ignore = "true"
	@priority = "5"
	test TranslationsRemainsUponSavingWebContent {
		property portal.acceptance = "false";

		// TODO LPS-123536 TranslationsRemainsUponSavingWebContent pending implementation
	}

	@description = "This is a test for LPS-123536. As Content Creator, I want to define reusable translations when creating WEM Structures with nested field"
	@priority = "4"
	test VerifyDefaultLanguageIsFirstShown {
		property portal.acceptance = "false";

		var structureTitle = "Structure Title";
		var structureTitlePT = "Titulo da Estrutura";
		var webTitle = "WC Title";

		WebContentNavigator.openWebContentAdmin(siteURLKey = "test-site-name");

		NavItem.gotoStructures();

		WebContentStructures.addCP(
			structureDescription = "I'm a structure test",
			structureName = "${structureTitle}");

		DataEngine.addField(
			fieldFieldLabel = "Image",
			fieldName = "Image");

		DataEngine.addFieldNested(
			fieldFieldLabel = "Numeric",
			fieldName = "Numeric",
			targetFieldLabel = "Image");

		Button.clickSave();

		WebContentNavigator.gotoEditStructure(structureName = "${structureTitle}");

		WebContentStructures.addLocalization(
			locale = "pt_BR",
			localization1Name = "${structureTitlePT}");

		FormViewBuilder.editFieldLabel(
			fieldLabel = "Fields Group",
			label = "Grupo de Campos");

		FormViewBuilder.editFieldLabel(
			fieldLabel = "Image",
			label = "Imagem");

		FormViewBuilder.editFieldLabel(
			fieldLabel = "Numeric",
			label = "Numero");

		Button.clickSave();

		WebContentNavigator.gotoEditStructure(structureName = "${structureTitle}");

		AssertTextEquals(
			locator1 = "Button#LOCALIZATION_GENERIC",
			value1 = "en-US");
	}

}